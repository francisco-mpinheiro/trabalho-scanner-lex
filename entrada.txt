/* 
 * Arquivo de teste atualizado para o Parser inicial.
 * No momento, a nossa gramatica em parser.cup suporta:
 * - Leitura de variaveis simples: read(x);
 * - Expressoes matematicas com precedencia e parenteses
 * - Impressao de expressoes: print(expr);
 */

/* Lendo uma variavel */
read(minhavariavel);

/* Expressoes matematicas basicas (operadores e precedencia) */
10 + 20 * 3;

(10 + 20) / 2;

minhavariavel - 5.5;

/* Testando o comando print com uma expressao complexa */
print(minhavariavel * 2 + 100.5);